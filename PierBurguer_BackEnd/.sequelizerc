const path = require('path');
// Aqui determinamos o caminho de onde a migration estara.
module.exports = {
  config: path.resolve(__dirname, 'config', 'config.json'),
  'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'),
};